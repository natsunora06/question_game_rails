<%= form_with(model: answer_history, local: true) do |form| %>
  <% if answer_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(answer_history.errors.count, "error") %> prohibited this answer_history from being saved:</h2>

      <ul>
      <% answer_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :question_id %>
    <%= form.number_field :question_id %>
  </div>

  <div class="field">
    <%= form.label :answer_id %>
    <%= form.number_field :answer_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
